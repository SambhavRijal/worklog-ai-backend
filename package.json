{
  "name": "worklog-ai-backend",
  "version": "0.1.0",
  "description": "WorkLog AI backend — finds your Slack 'before you start work' post, posts the 'finish work' reply, stores a structured record.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "init-db": "node src/db.js"
  },
  "dependencies": {
    "@slack/web-api": "^7.8.0",
    "better-sqlite3": "^11.8.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
